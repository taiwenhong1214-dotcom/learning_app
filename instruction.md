Firebase Integration (Data & Auth)
1. User Authentication: Implement Email registration/login so each user has their own learning profile.
2. Progress Tracking (Firestore): Save the user's learning progress (e.g., learned vocabulary) and Quiz history scores so data persists across app launches.
3. Global Leaderboard (Firestore): Create a query to fetch and display a ranking of all users based on their total Quiz scores.